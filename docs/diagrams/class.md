# Class Diagram

> Services, models, interfaces, and dependencies. Update on major refactors.

```mermaid
classDiagram
    %% Add classes as services and models are created
    %% Example:
    %% class AppwriteService {
    %%     +createSessionClient() Client
    %%     +createAdminClient() Client
    %% }
```
