# Use Case Diagram

> Actors and their interactions. Update when adding new features or user roles.

```mermaid
graph LR
    %% Add actors and use cases as features are built
    %% Example:
    %% User((User))
    %% Admin((Admin))
    %% User --> Login
    %% User --> Register
    %% User --> MakePayment
    %% Admin --> ManageUsers
```
